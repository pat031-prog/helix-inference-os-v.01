[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "helix-merkle-dag"
version = "0.1.0"
requires-python = ">=3.11"

[tool.maturin]
features = ["python"]
module-name = "helix_kv._helix_merkle_dag"

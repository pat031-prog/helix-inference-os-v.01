# Provider model audit note

This page has been replaced by the neutral provider metadata audit document:

[`docs/provider-model-audit.md`](provider-model-audit.md)

The supported claim is narrower and more precise: HeliX records the requested
model, the provider-returned model, digests, latency and lineage so model
mismatches are auditable. It does not infer hidden model identity or provider
intent.
